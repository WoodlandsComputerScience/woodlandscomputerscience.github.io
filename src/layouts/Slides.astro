---
import Post from "./Post.astro";
const url: string = Astro.props.frontmatter.slides;
---
<Post {...Astro.props}>
    <iframe class="rounded-2xl mt-4 w-full aspect-[1.55]" src={url} allowfullscreen="true" />
</Post>